# Packaged compiler lives here after scripts/pack-dist.ps1 (Windows) or scripts/pack-dist.sh (POSIX).
# `buraaq` is the self-hosted guest. Seed is compiler-buraaq/boot/stage0.ll or a previous guest binary.
