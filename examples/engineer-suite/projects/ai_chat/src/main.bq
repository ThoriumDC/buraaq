# Needs a running serve: buraaq ai serve MODEL
# Guest compile check: this file must build without a server.
module main

use std.io.println

fn main() {
    println("ai_chat: use buraaq ai serve, then chat")
}
